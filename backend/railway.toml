# Backend service config. In Railway UI set the service's
# "Root Directory" to `backend` so this file is picked up.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
